import assert from 'assert';
//import Filo from '../../src/filo.js';

describe('filo', () => {
  test('filo sort order (default)', () => {
    //const filo = new Filo();
    //console.log(filo.query("select * from books"));
    assert.equal('I will pass', 'I will pass');
  });
});